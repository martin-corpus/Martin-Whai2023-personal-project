import express from 'express'
// import { getWidgets, addNewWidget, deleteWidgetById, updateWidgetById } from '../db/db'
// import { NewWidget, Widget } from "../../models/Widget"
const router = express.Router()


// GET /api/v1/users
// router.get('/users', async (req, res) => {
//   try {
//     const widgets = await getUsers()
//     res.json({ widgets })
//   } catch(error) {
//     console.error(error)
//     res.sendStatus(500)
//   }
// })

export default router